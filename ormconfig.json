{
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "username": "postgres",
  "password": "docker",
  "database": "rentx",
  "synchronize": true,
  "logging": false,
  "entities": [
     "src/entity/**/*.ts"
  ],
  "migrations": [
     "./src/database/migrations/**/*.ts"
  ],
  "subscribers": [
     "src/database/subscriber/**/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
